# Optional SSO/SAML and LDAP authentication
# These packages require C++ build tools on Windows
# Install Microsoft Visual C++ Build Tools first:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/

# SAML authentication
python3-saml==1.16.0
xmlsec==1.3.13

# LDAP/Active Directory authentication
django-auth-ldap==4.6.0
# Note: django-auth-ldap requires python-ldap which needs C compiler

# For Windows users without build tools:
# - SAML will be disabled (use standard Django auth)
# - LDAP will be disabled (use standard Django auth)
# - All other features work normally
